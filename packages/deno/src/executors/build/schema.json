{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "cli": "nx",
  "title": "Build executor",
  "description": "",
  "type": "object",
  "properties": {
    "main": {
      "type": "string",
      "description": "The entry point for your Deno application"
    },
    "outputFile": {
      "type": "string",
      "description": "The destination of the bundled JavaScript"
    }
  },
  "required": ["main", "outputFile"]
}
